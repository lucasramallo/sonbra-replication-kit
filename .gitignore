# Dataset: não versionado (baixar do Zenodo — ver data/README.md)
data/base_sonbra/

# O fonte LaTeX do artigo não faz parte deste pacote — só o PDF compilado
# (artigo.pdf), que é versionado. Sem .tex aqui, não há build a ignorar.

# Python
__pycache__/
*.py[cod]
venv/
.venv/
